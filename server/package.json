{
  "name": "zoopaloola-match-server",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal two-player WebSocket room server for Zoopaloola",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
